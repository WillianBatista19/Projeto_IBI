# Adicionando as coordenadas (x, y) para o mouse
        #mouse_x, mouse_y = pygame.mouse.get_pos()
        
        # Definindo a ação ao clicar nos botões
        #if botaoVoltar.collidepoint((mouse_x, mouse_y)):
        #    if clique:
        #        pygame.quit()
        #if setaDireita.collidepoint((mouse_x, mouse_y)):
        #    if clique:
        #        telaEquipe2()

        #clique = False

        ################
        #if event.type == MOUSEBUTTONDOWN and event.button == 1